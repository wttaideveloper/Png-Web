# ── Root .env — read by docker-compose at BUILD time ──────────────────────────
# This only needs the frontend build arg.
# Backend secrets live in backend/.env.production

VITE_STRAPI_API_URL=https://yourdomain.com/api
